postal-sendgrid has been installed.

Set Plunk SENDGRID_API_BASE_URL to the in-cluster service URL:
http://{{ include "postal-sendgrid.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Configure Postal webhooks to POST to:
http://{{ include "postal-sendgrid.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}/webhooks/postal
